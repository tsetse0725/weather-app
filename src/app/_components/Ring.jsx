export const Ring = ({ el }) => {
  return (
    <div
      className={`z-50 absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 border border-gray-300 rounded-full ${el}`}
    ></div>
  );
};
